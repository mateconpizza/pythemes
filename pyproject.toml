[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pythemes"
dynamic = ["version"]
description = 'select desktop themes'
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
keywords = []
authors = [{ name = "mateconpizza", email = "81921095+mateconpizza@users.noreply.github.com" }]
classifiers = [
  "Development Status :: 4 - Beta",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: Implementation :: CPython",
  "Programming Language :: Python :: Implementation :: PyPy",
]
dependencies = []

[project.urls]
Documentation = "https://github.com/mateconpizza/pythemes#readme"
Issues = "https://github.com/mateconpizza/pythemes/issues"
Source = "https://github.com/mateconpizza/pythemes"

[project.scripts]
pythemes = "pythemes.__main__:main"

[tool.hatch.version]
path = "pythemes/__main__.py"

[[tool.mypy.overrides]]
module = ["pytest.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
pythonpath = "pythemes"
testpaths = ["tests"]

[tool.ruff]
fix = false
line-length = 100
show-fixes = true
target-version = "py311"
exclude = [".venv", ".mypy_cache", ".pytest_cache"]

[tool.ruff.format]
quote-style = "single"
line-ending = "auto"
skip-magic-trailing-comma = false
docstring-code-format = true

[tool.ruff.lint]
dummy-variable-rgx = "^(_+|(_+[a-zA-Z0-9_]*[a-zA-Z0-9]+?))$"
task-tags = ["HACK", "FIX", "BUG"]
select = [
  "A",
  "ARG",
  "B",
  "C",
  "DTZ",
  "E",
  "EM",
  "F",
  "FBT",
  "I",
  "ICN",
  "N",
  "PLC",
  "PLE",
  "PLR",
  "PLW",
  "RUF",
  "S",
  "T",
  "TID",
  "UP",
  "W",
  "YTT",
]
extend-select = [
  "B",   # (flake8-bugbear)
  "BLE", # (flake8-blind-except)
  "C4",  # (flake8-comprehensions)
  # "ERA",  # (eradicate)
  "I",   # (isort)
  "ICN", # (flake8-import-conventions)
  "INP", # (flake8-no-pep420)
  "N",   # (pep8-naming)
  "NPY", # (numpy-specific-rules)
  "PGH", # (pygrep-hooks)
  "PIE", # (flake8-pie)
  "PTH", # (flake8-use-pathlib)
  # "Q",   # (flake8-quotes)
  "RET", # (flake8-return)
  "RSE", # (flake8-raise)
  "RUF", # (ruff-specific-rules)
  "S",   # (flake8-bandit)
  "SIM", # (flake8-simplify)
  "T20", # (flake8-print)
  "TCH", # (flake8-type-checking)
  "TID", # (flake8-tidy-imports)
  "YTT", # (flake8-2020)
]
ignore = ["Q000", "T201", "FBT001", "FBT002"]

[tool.ruff.lint.isort]
force-single-line = true
order-by-type = true
required-imports = ["from __future__ import annotations"]

[tool.ruff.lint.flake8-pytest-style]
fixture-parentheses = false
mark-parentheses = true
parametrize-names-type = "tuple"
raises-require-match-for = ["requests.RequestException"]

[tool.ruff.lint.per-file-ignores]
"tests/**/*" = [
  "A003",   # builtin-attribute-shadowing
  "ARG002", # unused-method-argument
  "FBT001", # boolean-type-hint-positional-argument
  "FBT002", # boolean-default-value-positional-argument
  "Q000",   # single-quote preferred
  "S101",   # use of `assert` detected
]

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "all"

[dependency-groups]
dev = [
    "coverage[toml]>=6.5",
    "pytest>=8.3.5",
]
lint = [
    "mypy>=2.3.1",
    "ruff>=0.16.8",
]

[tool.hatch.build.targets.sdist]
exclude = [".pre-commit-config.yaml", ".gitignore"]

[tool.hatch.build.targets.wheel]
exclude = [".pre-commit-config.yaml", ".gitignore"]
